{
  "name": "server",
  "version": "1.0.0",
  "description": "AI Automation Hub backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd ../client && npm ci --omit=dev && npm run build",
    "test": "npm run test:jest && npm run test:node",
    "test:jest": "jest index.test.js --runInBand",
    "test:node": "node --test test/server.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.12.2",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
